import { createWebhook } from './lib/runtime.ts';
import { eligibilityPolicyFromEnv } from '../_shared/pilot-registration/eligibility.ts';
const env=(name:string)=>Deno.env.get(name)??'';
Deno.serve(createWebhook({url:env('SUPABASE_URL'),serviceKey:env('SUPABASE_SERVICE_ROLE_KEY'),watcherKey:env('ULSAN_WATCHER_KEY'),operatorKey:env('ULSAN_OPERATOR_KEY'),botToken:env('TELEGRAM_BOT_TOKEN'),chatId:env('TELEGRAM_CHAT_ID'),webhookSecret:env('TELEGRAM_WEBHOOK_SECRET'),adminChats:env('TELEGRAM_ADMIN_CHAT_IDS').split(',').map(s=>s.trim()).filter(Boolean),botUsername:env('TELEGRAM_BOT_USERNAME'),gatewayJwt:env('ULSAN_GATEWAY_JWT')||env('SUPABASE_ANON_KEY'),miniAppEnabled:env('PILOT_MINIAPP_ENABLED')==='true',registration:{eligibilityPolicy:eligibilityPolicyFromEnv(env('ULSAN_PILOT_ELIGIBILITY_POLICY')),username:env('ULSAN_PILOT_USERNAME'),password:env('ULSAN_PILOT_PASSWORD'),sessionKey:env('ULSAN_SESSION_KEY'),transport:env('ULSAN_TRANSPORT')==='http-approved'?'http-approved':'https',createEnabled:env('ULSAN_PILOT_REGISTRATION_ENABLED')==='true',updateEnabled:env('ULSAN_PILOT_UPDATE_ENABLED')==='true'}}));
